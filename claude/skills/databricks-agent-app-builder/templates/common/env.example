# Make a copy of this to set environment variables for local development
# cp .env.example .env.local

# Authentication (choose one method)
# Option 1: OAuth profile (recommended)
DATABRICKS_CONFIG_PROFILE=DEFAULT

# Option 2: Token-based (alternative)
# DATABRICKS_HOST=https://<your-workspace>.cloud.databricks.com
# DATABRICKS_TOKEN=dapi....

# MLflow Configuration (required)
MLFLOW_EXPERIMENT_ID=
MLFLOW_TRACKING_URI="databricks"
MLFLOW_REGISTRY_URI="databricks-uc"

# Frontend Configuration (optional, for conversational agents with UI)
# API_PROXY=http://localhost:8000/invocations
# CHAT_APP_PORT=3000
# CHAT_PROXY_TIMEOUT_SECONDS=300
